apiVersion: v2
name: accounts
description: Accounts microservice — owns user accounts, balances and per-user data.
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: common-microservice
    version: 0.1.0
    repository: "file://../common-microservice"
  - name: postgres
    version: 0.1.0
    repository: "file://../postgres"
